#include <iostream>
#include <bits/stdc++.h>
#define ll long long int
using namespace std;
int main()
{
    int n, q;
    cin >> n >> q;
    for (int i = 0; i < q; i++)
    {
        int t;
        cin >> t;
        if (t == 1 || t == 2)
        {
            int x, y;
            cin >> x >> y;
            
        }
        if (t == 3)
        {
            int x1, y1, x2, y2;
            cin >> x1 >> y1 >> x2 >> y2;
        }
    }
    return 0;
}